# 按钮统一性验证 (Button Consistency Verification)

## ✅ 修改完成

### 最终阶段按钮统一为 "✓ Submit Data"

两个实验条件在最终阶段都显示相同的按钮文本，确保用户体验一致。

---

## 📱 完整按钮流程

### **Condition 1: puzzleFirst**

#### 阶段 1: Task 完成（中间阶段）
```
┌──────────────────────────────┐
│     Task Completion Modal    │
│   显示分数: 15/20             │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │ → Continue to Free Play│  │
│  │      (绿色 #10b981)    │  │
│  └────────────────────────┘  │
│                              │
│ "Your progress has been      │
│  saved."                     │
└──────────────────────────────┘
       ↓ 点击按钮
   保存 taskData → 跳转到 Freeplay
```

#### 阶段 2: Freeplay 完成（**最终阶段**）
```
┌──────────────────────────────┐
│  Freeplay Completion Modal   │
│   显示创作的作品              │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │   ✓ Submit Data        │  │
│  │     (蓝色 #3b82f6)     │  │
│  └────────────────────────┘  │
│                              │
│ "Your complete experiment    │
│  data will be submitted."    │
└──────────────────────────────┘
       ↓ 点击按钮
   保存 freeplayData → 提交到服务器
```

---

### **Condition 2: freeplayFirst**

#### 阶段 1: Freeplay 完成（中间阶段）
```
┌──────────────────────────────┐
│  Freeplay Completion Modal   │
│   显示创作的作品              │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │ → Continue to Puzzle   │  │
│  │      Phase             │  │
│  │    (绿色 #10b981)      │  │
│  └────────────────────────┘  │
│                              │
│  (无提示信息)                │
└──────────────────────────────┘
       ↓ 点击按钮
   保存 freeplayData → 跳转到 Task
```

#### 阶段 2: Task 完成（**最终阶段**）
```
┌──────────────────────────────┐
│     Task Completion Modal    │
│   显示分数: 15/20             │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │   ✓ Submit Data        │  │
│  │     (蓝色 #3b82f6)     │  │
│  └────────────────────────┘  │
│                              │
│ "Your complete experiment    │
│  data will be submitted."    │
└──────────────────────────────┘
       ↓ 点击按钮
   保存 taskData → 提交到服务器
```

---

## 🎯 关键一致性

### ✅ 最终阶段统一

| 条件 | 最终阶段 | 按钮文本 | 按钮颜色 | 提示信息 | 行为 |
|------|---------|---------|---------|---------|------|
| puzzleFirst | Freeplay | `✓ Submit Data` | 蓝色 #3b82f6 | "Your complete experiment data will be submitted." | 提交到服务器 |
| freeplayFirst | Task | `✓ Submit Data` | 蓝色 #3b82f6 | "Your complete experiment data will be submitted." | 提交到服务器 |

### ✅ 中间阶段区分

| 条件 | 中间阶段 | 按钮文本 | 按钮颜色 | 提示信息 | 行为 |
|------|---------|---------|---------|---------|------|
| puzzleFirst | Task | `→ Continue to Free Play` | 绿色 #10b981 | "Your progress has been saved." | 跳转到 Freeplay |
| freeplayFirst | Freeplay | `→ Continue to Puzzle Phase` | 绿色 #10b981 | 无 | 跳转到 Task |

---

## 💡 设计理念

### 颜色语义
- **绿色** (#10b981)：表示"继续/下一步"，还有更多内容
- **蓝色** (#3b82f6)：表示"提交/完成"，这是最后一步

### 文本清晰度
- **中间阶段**：明确告诉用户要去哪里（"Continue to Free Play" / "Continue to Puzzle Phase"）
- **最终阶段**：统一显示 "Submit Data"，用户清楚知道实验结束并提交数据

### 用户反馈
- 两个条件的最终阶段都显示相同的提示："Your complete experiment data will be submitted."
- 如果提交失败，会自动下载数据文件作为备份

---

## 🔍 代码实现位置

### Task 阶段按钮更新
**文件**: `js/task.js` - `showCompletionModal()` 函数

```javascript
if (condition === 'freeplayFirst') {
    // freeplayFirst: This is the final phase - show submit button
    if (continueBtn) {
        continueBtn.innerHTML = '✓ Submit Data';
        continueBtn.style.background = '#3b82f6'; // Blue for submit
    }
    if (continueMessage) {
        continueMessage.textContent = 'Your complete experiment data will be submitted.';
    }
} else {
    // puzzleFirst: Continue to freeplay phase
    if (continueBtn) {
        continueBtn.innerHTML = '→ Continue to Free Play';
        continueBtn.style.background = '#10b981'; // Green for continue
    }
    if (continueMessage) {
        continueMessage.textContent = 'Your progress has been saved.';
    }
}
```

### Freeplay 阶段按钮更新
**文件**: `js/freeplay.js` - `endFreePlayMode()` 函数

```javascript
if (condition === 'freeplayFirst') {
    submitBtn.innerHTML = '→ Continue to Puzzle Phase';
    submitBtn.style.background = '#10b981'; // Green for continue
} else {
    submitBtn.innerHTML = '✓ Submit Data';
    submitBtn.style.background = '#3b82f6'; // Blue for submit
}
```

---

## ✅ 验证清单

### puzzleFirst 条件
- [ ] Task 完成显示 "→ Continue to Free Play"（绿色）
- [ ] 点击按钮跳转到 Freeplay
- [ ] Freeplay 完成显示 "✓ Submit Data"（蓝色）
- [ ] 点击按钮提交数据到服务器
- [ ] 显示提示 "Your complete experiment data will be submitted."

### freeplayFirst 条件
- [ ] Freeplay 完成显示 "→ Continue to Puzzle Phase"（绿色）
- [ ] 点击按钮跳转到 Task
- [ ] Task 完成显示 "✓ Submit Data"（蓝色）
- [ ] 点击按钮提交数据到服务器
- [ ] 显示提示 "Your complete experiment data will be submitted."

### 一致性验证
- [ ] 两个条件的最终阶段按钮文本完全一致
- [ ] 两个条件的最终阶段按钮颜色完全一致
- [ ] 两个条件的最终阶段提示信息完全一致
- [ ] 中间阶段按钮都是绿色
- [ ] 最终阶段按钮都是蓝色
